title: Organization dashboard visibility
type: string
description: >
  Visibility of an organization dashboard.
  `PUBLIC` — visible to all organization members with access to Organization dashboards.
  `PRIVATE` — visible only to the owner (reserved for a later iteration; create always uses `PUBLIC`).
enum:
  - PUBLIC
  - PRIVATE
default: PUBLIC
