title: ProjectDetails
x-stoplight:
  id: msbxfonb90si2
type: object
properties:
  role:
    type: string
    x-stoplight:
      id: k7uxpi9vxvg0g
    enum:
      - EDITOR
      - VIEWER
    description: |-
      User's role in the project.
      All users with a `PROJECT_MANAGER` role have
      the `EDITOR` role by default.
